# Welcome to Words → Art

This is a creative tool that transforms your text into unique visual art through binary conversion.

## How It Works

1. You enter any text in the input field
2. The system converts each character to its 8-bit binary representation
3. The binary string is used to generate RGB pixel values
4. These pixels form an abstract image

The result is always unique to your input!
